export const usageString = `usage:
a2s init # Generate config file
a2s generate [-y] # Generate/Update the generated files, '-y' overwrite the existing files without confirm
a2s help # Print this help message
`
