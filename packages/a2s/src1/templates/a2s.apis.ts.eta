/* eslint-disable */
import type { Apis } from './a2s.request'

export const apis: Apis = {
<% it.apis.forEach(function(api){ %>
  '<%= api.group %>@<%= api.title %>': {
    u: '<%= api.path %>',
    m: '<%= api.method %>',
    <% if(api.req_params && api.req_params.length > 0) {
    %>p: [<% api.req_params.forEach(function(p) { %>'<%= p.name %>', <% } ) %>],
    <% } %>d: <% if(api.status === 'done') { %>1<% } else { %>0<% } %>,
  },
<% }) %>
}
