export const usageString = `usage:
dc request init # Generate config file
dc request update [-y] [--js] # Generate/Update the service files, '-y' overwrite the existing files without confirm, '--js' generate '.js' files rather than '.ts' files
dc request help # Print this help message
`
